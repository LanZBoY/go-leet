---
description: LeetCode 出題與作答流程——先驗測資、不留答案、作答只給提示
alwaysApply: true
---

# LeetCode Practice Workflow

## 生成題目檔案時（`problems/p*/`）

1. **先寫標準答案**：在 `solution.go` 實作完整解法。
2. **跑測試驗證測資**：`go test -v` 確認 `solution_test.go` 的 expected 正確。
3. **測資通過後刪除答案**：將 `solution.go` 改為僅留函式簽名 + `panic("not implemented")`（或等效 stub）。
4. **測試檔只測主函式**：不要留下 `sln_*` 多解法或任何參考實作。
5. **交付給使用者時**：只提供題目骨架與測資，不附上解法說明或完整程式碼。

## 使用者自行作答時

- **只給引導或提示**：例如思路方向、邊界條件、複雜度考量、相關資料結構。
- **不要直接給答案**：不貼完整解法、不寫出可直接複製通過的程式碼。
- 使用者明確要求「給答案」「看解答」時，才可提供完整實作。

## 測資慣例

- 含 LeetCode 官方範例。
- 加邊界案例（最小輸入、全相同、遞增/遞減、極值等）。
- 格式對齊既有題目（如 `p1929_concatenation_of_array` 的 `solution_test.go` 風格）。
